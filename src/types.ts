export interface MagazineIssue {
  id: string;
  issueNumber: string;
  headline: string;
  sceneDescription: string;
  imageUrl: string;
  createdAt: number;
  userId: string;
  authorName: string;
}
